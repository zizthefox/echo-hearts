[project]
name = "echo-hearts"
version = "0.1.0"
description = "AI Companion RPG with emergent relationships via MCP"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gradio>=5.49.1",
    "openai>=1.0.0",
    "anthropic>=0.18.0",
    "python-dotenv>=1.0.0",
    "mcp>=0.1.0",
]
